# true  = use the in-browser mock API (default in `npm run dev`)
# false = call the real backend
VITE_USE_MOCK=true

# Base URL of the portal backend. Leave empty to use same-origin /api/...
VITE_API_BASE_URL=
